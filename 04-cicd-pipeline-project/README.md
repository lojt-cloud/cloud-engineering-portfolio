# CI/CD Pipeline with GitHub Actions

## What this is
A CI/CD pipeline using GitHub Actions to automate testing, security scanning (Trivy/Checkov),
and deployment of infrastructure or application code.

## Pipeline overview
See `.github/workflows/` for the pipeline definitions.

## What I learned
-

## Architecture
Push to GitHub → GitHub Actions triggers → tests/scans run → deploy step
