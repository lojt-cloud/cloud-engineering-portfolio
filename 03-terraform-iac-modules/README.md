# Reusable Terraform Modules

## What this is
A collection of reusable Terraform modules built while learning Infrastructure as Code —
covering state management, variables, and module composition.

## Modules included
- `modules/` — individual reusable modules
- `examples/` — example usage of each module

## What I learned
-

## How to run it
```bash
cd examples/<example-name>
terraform init
terraform plan
```
